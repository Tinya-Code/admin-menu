export interface Combo {
  id: string;
  name: string;
  description: string;
  price: number;
  created_at: string;
  updated_at: string;
}

export interface ComboForm {
  name: string;
  description: string;
  price: number;
}
